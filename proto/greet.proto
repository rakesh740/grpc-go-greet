syntax = "proto3";

package greet;

option go_package = "projects/Greet/proto";

message operand {
    uint32 firstNum = 1;
    uint32 secondNum = 2;

}
message response {
    uint32 answer = 1;
}

service Calculator {
    rpc sum (operand) returns (response);
} 